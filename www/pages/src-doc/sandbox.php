<div class="container py-2 px-0"><h4 id="sandbox_run/5"><a href="/fasill/documentation/src/sandbox#sandbox_run/5">sandbox_run/5</a></h4><?php echo show_template("sandbox_run(+Program, +Lattice, +Sim, +Goal, +Limit)"); ?><p><?php echo show_description("This predicate loads the program <+Program, +Lattice, +Sim> into the environment and runs the goal +Goal, with a limit of derivations +Limit, and writes in the standard output the resulting derivations."); ?></p></div>
<div class="container py-2 px-0"><h4 id="sandbox_listing/1"><a href="/fasill/documentation/src/sandbox#sandbox_listing/1">sandbox_listing/1</a></h4><?php echo show_template("sandbox_listing(+Program)"); ?><p><?php echo show_description("This predicate loads the program +Program into the environemnt and, and writes in the standard output the loaded rules."); ?></p></div>
<div class="container py-2 px-0"><h4 id="sandbox_unfold/4"><a href="/fasill/documentation/src/sandbox#sandbox_unfold/4">sandbox_unfold/4</a></h4><?php echo show_template("sandbox_unfold(+Program, +Lattice, +Sim, +Rule)"); ?><p><?php echo show_description("This predicate loads the program <+Program, +Lattice, +Sim> into the environemnt and runs the unfolding of the rule +Rule."); ?></p></div>
<div class="container py-2 px-0"><h4 id="sandbox_tune/5"><a href="/fasill/documentation/src/sandbox#sandbox_tune/5">sandbox_tune/5</a></h4><?php echo show_template("sandbox_tune(+Program, +Lattice, +Sim, +Tests, +Limit)"); ?><p><?php echo show_description("This predicate loads the program <+Program, +Lattice, +Sim> into the environment and tune the program w.r.t. the set of test cases +Tests, with a limit of derivations +Limit, and writes in the standard output the resulting substitution."); ?></p></div>
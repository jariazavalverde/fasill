<div class="container py-2 px-0"><h4 id="findall/3"><a href="/fasill/documentation/ref/all-solutions#findall/3">findall/3</a></h4><p class="text-secondary">Find all the values that would make a goal succeed.</p><?php echo show_template("findall( ?term, +callable_term, ?list )"); ?><p><?php echo show_description("findall(Template, Goal, Instances) is true if and only if Instances is a list of values in the form Templates that would make the goal Goal succeed. Usually, Template and Goal share some variables, so Instances is filled with the values that make Goal succeed. If there is not a single value that make Goal unify, Instances will be an empty list."); ?></p></div>